{
  "name": "experience-platform-app",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build:web": "bash build-web.sh",
    "cap:init": "npx cap init",
    "cap:sync": "npx cap sync",
    "cap:open": "npx cap open android",
    "cap:build": "npx cap copy && cd android && ./gradlew assembleDebug",
    "build:apk": "npm run cap:sync && npm run cap:build"
  },
  "dependencies": {
    "@capacitor/core": "^6.1.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.1.0",
    "@capacitor/android": "^6.1.0"
  }
}
